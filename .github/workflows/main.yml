name: Build and Deploy EPIDEMIA Stack

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build-frontend-image:
    permissions:
      contents: read
      packages: write

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: benjlevesque/short-sha@v3.0
      id: short-sha
      with:
        length: 6

    - name: Build frontend image
      run: |
        docker build . \
          --file ./Dockerfile \
          --build-arg REACT_APP_FORECAST_API_BASE=https://epidemia-forecast-api.disc.ourcloud.ou.edu \
          --build-arg REACT_APP_ENV_API_BASE=https://epidemia-env-api.disc.ourcloud.ou.edu \
          --tag ghcr.io/ou-disc/epidemia-ui:$SHA
      env:
          SHA: ${{ steps.short-sha.outputs.sha }}

    - name: 'Login to GitHub Container Registry'
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{github.actor}}
        password: ${{secrets.GITHUB_TOKEN}}

    - name: Push images
      run: |
        docker push ghcr.io/ou-disc/epidemia-ui:$SHA
      env:
          SHA: ${{ steps.short-sha.outputs.sha }}


  deployment:
    needs:
      - build-frontend-image
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@v4

    - uses: benjlevesque/short-sha@v3.0
      id: short-sha
      with:
        length: 6

    ################################################################################################################
    #
    # The deploy job requires a kubeconfig. To use that, take the kubeconfig file from
    # ~/.kube/config and edit the IP address in there to:
    # server: https://disc.ourcloud.ou.edu:6443
    #
    # Then copy the file contents and add it to the project -> settings -> variables -> actions
    # Name the variable KUBECONFIG and paste the contents as the value.
    #
    ################################################################################################################
    - name: WriteKubeConfig
      run: |
        mkdir -p ~/.kube
        echo "$KUBECONFIG_CONTENT" > ~/.kube/config
      env:
        KUBECONFIG_CONTENT: '${{ secrets.KUBECONFIG }}'

    - uses: helmfile/helmfile-action@v2.4.0
      with:
        helm-plugins: >
          https://github.com/databus23/helm-diff,
          https://github.com/jkroepke/helm-secrets
        helmfile-args: apply
      env:
        SHA: ${{ steps.short-sha.outputs.sha }}